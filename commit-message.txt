feat: Added advanced edit-post UI with prefilled fields and image preview

- Introduced `views/edit-post file` to allow secure post editing
- Preloads post data using dynamic ID (GET param)
- Includes CSRF protection and hidden input for post ID
- Responsive, modern UI with Tailwind + dark mode support
- Existing image preview with option to replace
- Fields: title, description, category, status, and image upload
- Ready for backend update handler integration (`modules/posts/update.php`)
- Built to support future enhancements (WYSIWYG, validation, autosave, etc.)
- ALso SOme more updates in other files.
- Some more files added for update post working.

