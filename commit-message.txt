🔄 Implemented Advanced Post Update Functionality (Update - CRUD)

✅ Built full post update flow with modern UI & future-proof backend:
- Created `/views/edit-post.php` page with Tailwind-powered UI
- Added secure CSRF token protection (session-based)
- Used PDO prepared statements to safely fetch/update post by ID
- Pre-filled form fields with existing post data
- Integrated image preview, conditional replacement & deletion
- Preserved existing image if no new file uploaded
- Auto-generated slug if title is updated (slug regeneration logic)
- Updated `update.php` in `modules/posts/` to handle:
  - Input sanitization and validation
  - Old image deletion (via helper)
  - Error-safe DB update with try-catch block
- Clean URL routing with error fallback: `/edit-post.php?id={id}`
- Displayed user-friendly error if post not found or invalid ID

🛠 Fixed critical bug:
- Corrected argument order in `generateUniqueSlug()` to match definition
  → Now uses: `$slug = generateUniqueSlug($title, $post_id, $pdo);`

💡 Future Ready:
- Dynamic user_id binding via session
- WYSIWYG/Markdown ready `<textarea>`
- Future live AJAX edit via modal (optional enhancement)
- Future toast alerts & validation feedback system

